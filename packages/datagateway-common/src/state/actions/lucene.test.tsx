jest.mock('../../handleICATError');

describe('Lucene actions', () => {
  it.todo('builds a query');

  it.todo('fetches lucene data');

  describe('fetchLuceneIds actions', () => {
    it.todo(
      'dispatches fetchLuceneIdsRequest and fetchLuceneIdsSuccess actions upon successful fetchLuceneIds action'
    );
  });
});
