{
  "name": "datagateway",
  "private": true,
  "version": "1.1.0",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "husky": "8.0.1",
    "lerna": "5.5.0"
  },
  "devDependencies": {
    "lerna-update-wizard": "1.1.0"
  },
  "scripts": {
    "build": "yarn workspaces foreach --parallel --exclude datagateway-common run build",
    "lint": "yarn workspaces foreach --parallel run lint:js --max-warnings=0",
    "tsc": "yarn workspace datagateway-common tsc",
    "test": "yarn test:unit && yarn test:e2e",
    "test:unit": "yarn workspaces foreach --verbose --exclude datagateway run test",
    "test:e2e": "cd packages/datagateway-dataview && yarn run e2e && cd ../datagateway-download && yarn run e2e && cd ../datagateway-search && yarn run e2e",
    "test:e2e:lerna": "lerna run e2e --stream --concurrency 1",
    "datagateway-dataview": "cd packages/datagateway-dataview && yarn start",
    "datagateway-download": "cd packages/datagateway-download && yarn start",
    "datagateway-search": "cd packages/datagateway-search && yarn start",
    "postinstall": "husky install"
  },
  "packageManager": "yarn@3.2.0"
}